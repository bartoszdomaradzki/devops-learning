# Moje pierwsze polecenia Git

- git init – tworzy nowe repozytorium
- git status – pokazuje status plików
- git add <plik> – dodaje plik do obszaru do zatwierdzenia
- git commit -m "komentarz" – zapisuje zmiany w repo
